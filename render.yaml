@"
services:
  - type: web
    name: yourtube-clone
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
"@ | Set-Content render.yaml